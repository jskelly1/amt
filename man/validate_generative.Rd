% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generative_validation.R
\name{validate_generative}
\alias{validate_generative}
\title{Validate generative behavior of movement simulations}
\usage{
validate_generative(
  observed,
  simulated,
  metrics = c("ud", "msd", "straightness", "barrier"),
  barrier = NULL,
  ud_args = list(),
  msd_args = list(),
  straightness_args = list(),
  barrier_args = list(),
  ...
)
}
\arguments{
\item{observed}{Observed track-like object. Supported inputs include
\code{track_xy}, \code{track_xyt}, \code{steps_xyt}, \code{steps_xy}, and data frames with
\code{x_} and \code{y_} columns.}

\item{simulated}{A list of simulated track-like objects, or a data frame with
one row per simulated location and a simulation identifier column.}

\item{metrics}{\verb{[character]} Metrics to compute. Supported values are
\code{"ud"}, \code{"msd"}, \code{"straightness"}, \code{"sinuosity"}, and \code{"barrier"}.
\code{"sinuosity"} is accepted as an alias for \code{"straightness"}.}

\item{barrier}{Optional \code{sf} LINESTRING or MULTILINESTRING object used for
barrier crossing validation.}

\item{ud_args, msd_args, straightness_args, barrier_args}{Lists of additional
arguments passed to the corresponding metric functions.}

\item{...}{Passed to simulation coercion helpers.}
}
\value{
An object of class \code{amt_generative_validation}.
}
\description{
Runs trajectory-level generative validation diagnostics by comparing an
observed track with simulated tracks. The fitted model is not required by
this function; users fit and simulate with \code{amt} or another workflow, then
pass the observed and simulated trajectories here.
}
\details{
Coordinates are treated as planar Euclidean coordinates. Longitude/latitude
CRS are rejected when the CRS is known. If no CRS is available, coordinates
are assumed to be planar and already comparable across observed, simulated,
and barrier data.
}
\examples{
observed <- track(c(0, 1, 1, 2), c(0, 0, 1, 1))
simulated <- list(
  track(c(0, 1, 2, 3), c(0, 0, 0, 0)),
  track(c(0, 0, 1, 1), c(0, 1, 1, 2))
)
res <- validate_generative(
  observed,
  simulated,
  metrics = c("msd", "straightness"),
  msd_args = list(max_lag = 2)
)
summary(res)
}
\references{
Nicosia, A. (2026). Beyond the next step: A multi-criteria generative
validation framework for step selection functions. \emph{Methods in Ecology and
Evolution}. \doi{10.1111/2041-210x.70313}
}
